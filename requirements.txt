# Runtime dependencies for the API only — this is what Azure Functions installs
# on deploy, so it is kept as small as the service actually needs. Everything
# used to build the data (pandas, scikit-learn, the Azure AI SDKs) lives in
# requirements-dev.txt and never reaches the server.
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
azure-functions>=1.21,<2
click==8.5.0
exceptiongroup==1.3.1
fastapi==0.141.1
h11==0.16.0
idna==3.19
pydantic==2.13.5
pydantic_core==2.46.5
pymssql==2.4.1
python-dotenv==1.2.3
sniffio==1.3.1
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
uvicorn==0.52.4
